#include<iostream>
using namespace std;
int main(){
    // to count frequency of an element in an array
    // int freq = count(arr,arr+len,element);

    //location of an element
    //int index = find(arr,arr+len, element)-arr;

    // note: if element is not fount then this fucntion return size of an array

    // array equality
    // bool ans = equal(arr1,arr+len,arr2);

    //reverse array
    //reverse(nums, nums+len); 

    


}