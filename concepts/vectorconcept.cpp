#include<iostream>
using namespace std;
int main(){
    //to count vector element frequency
    //int frq  = count(arr.begin(),arr.end(),20);

    // removing element
    //arr.erase(v.begin()+index);

    //adding element at index and rest value move forward
    //arr.insert(vec.begin()+index,val);

    //location of element
    //int idx = find(v.begin(),v.end(),element) - v.begin();  // if not present size of vector return;

    //reverse of an array
    //reverse(v.begin(),v.end());

    //sum max min of an vector;
    //int sum = accumulate(v.begin(),v.end(),sum_initiial);
    //int max = *max_element(v.begin(),v.end());
    //int min = *min_element(v.begin(),v.end());
   
    // sort in vector
    //


    //revesre in vector
    //






}